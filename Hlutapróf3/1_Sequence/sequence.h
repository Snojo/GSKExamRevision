#ifndef SEQUENCE_H
#define SEQUENCE_H

// This function calculates and writes out all the values in the following sequence (n >= 1):
// a(1) = 6
// a(n) = 0.5 * a(n-1) + 4

double sequence(int n);

#endif
